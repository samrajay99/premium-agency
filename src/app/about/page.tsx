import { Breadcrumbs } from "@/components/seo/Breadcrumbs";
import { SiteShell } from "@/components/layout/SiteShell";
import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata({ title: "About the Directory", description: "Learn how this fictional Hyderabad directory is structured, written, and designed for privacy-first discovery.", pathname: "/about" });
export default function AboutPage() { return <SiteShell><Breadcrumbs items={[{ name: "About", href: "/about" }]} /><p className="eyebrow">The approach</p><h1 className="display-title">A directory that explains its limits.</h1><div className="prose-copy mt-10 max-w-3xl"><p>Hyderabad Elite Directory is a fictional product concept for exploring how a local directory can feel calm, useful, and clear. The profiles, reviews, badges, and contact details are sample content.</p><h2>Privacy by default</h2><p>Pages avoid private addresses and identity documents. The contact interaction stays in the browser, and the project has no accounts, analytics SDKs, or backend.</p><h2>A useful city index</h2><p>Neighbourhood pages are deliberately differentiated. Gachibowli, Madhapur, the central hills, and Secunderabad each get context so internal links serve readers, not just crawlers.</p><h2>Verification without overclaiming</h2><p>Demo badges are labelled as demo badges. They do not imply identity checks, endorsements, or real-world availability.</p></div></SiteShell>; }